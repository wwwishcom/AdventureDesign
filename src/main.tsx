import * as React from "react";
import ReactDOM from "react-dom/client";
import BusIngApp from "./BusIngApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BusIngApp />
  </React.StrictMode>
);
